import styled from '@emotion/styled';

export const Wrapper = styled.div({
  position: 'absolute',
  padding: '8px',
  borderRadius: '8px',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  backgroundColor: 'white',
  boxShadow:
    '0px 8px 24px rgba(0, 0, 0, 0.1), 0px 4px 8px rgba(0, 0, 0, 0.1), 0px 2px 2px rgba(0, 0, 0, 0.1)',
  right: '-55',
  bottom: '35',
  width: 'auto',
});
